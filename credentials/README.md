# Credentials in Repository

This folder contains sanitized templates only.

Live credentials were removed from the repository as part of Step 4 secrets hardening.
Use `local_secrets/` for real credentials and tokens.
