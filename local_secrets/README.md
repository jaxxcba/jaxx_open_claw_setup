# Local Secrets (Do Not Commit)

Store live credentials and tokens in this folder only.

Rules:
- Keep this directory local.
- Do not commit any credential files.
- Use example templates under `credentials/examples/` as input shapes.
